using Blog_Platform.Models;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Blog_Platform.Data.Configure
{
    //public class RevokeConfigure : IEntityTypeConfiguration<Revoke>
    //{
    //    public void Configure(EntityTypeBuilder<Revoke> builder)
    //    {
    //        builder.HasKey(x => x.Id);
    //        builder.Property(x => x.Id).HasColumnName("Id").HasColumnType("int");
    //        builder.Property(x => x.Token).HasColumnName("Name").HasColumnType("varchar").IsRequired(true);
    //        builder.HasIndex(x => x.Token).IsUnique(true);
    //    }
    //}
}
